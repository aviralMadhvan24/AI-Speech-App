"""Body Language analyzers.

One file per analyzer (posture, eye_contact, gesture, stillness,
facial_expression) lands here in tasks 6.1 through 10.1. Each analyzer
is a pure function ``analyze(frames, cfg) -> MetricResult``.
"""
